[project]
name = "dnt-watcher"
version = "0.1.0"
description = "A cabin availability checker for DNT cabins"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "pyyaml>=6.0",
    "colorama>=0.4.6",
]
