[project]
name = "dnt-core"
version = "1.0.0"
description = "Core business logic for DNT cabin availability monitoring"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
